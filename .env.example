# Copy to ignored .env.local when a private source directory is available.
# This is not a secret, but it is a machine-local locator and must not point at a public delivery directory.
JESSICA_PRIVATE_SOURCE_ROOT=
